# Smoke/demo findings — 2026-07-22

Raw notes from the first-shop demo (transcribed verbatim).

## Raw log

```
Demo
liked locations setting - add location to inventory page
Want to do online
Need to include Chinese/Japanese/korean
Pricing feature for conditions
Add product feature is broken


Live auctions:
QR codes - scan all cards before stream to remove them from inventory prior to stream - figure how scanner would actually work

Want to connect it to their card payment system

Disney lore carna
One piece
Magic the gathering
Yu gi oh
Tops / panini

IDEA
Display tablet - for customer use
customer puts in details - sets them up as user which also translate to the POS
They select all the ones they want and it send the list to the POS system
Shows them a potential estimate
```

## Triage

| Item | Kind | Outcome |
|---|---|---|
| "liked locations setting — add location to inventory page" | tweak | Batched to a worktree subagent: visible Location column in InventoryTable (field exists, was hidden in the expandable detail row) |
| "Add product feature is broken" | bug | **Confirmed + root-caused, chip created.** Flow works end-to-end at HEAD (live repro: POST /api/products 201, row on /inventory). Failure mode: the Card/Product toggle on app/(app)/inventory/add/page.tsx silently drops clicks before React hydration — on a dev server the first visit paints seconds before it hydrates, which is exactly the demo setup. Demo DB confirms zero products created during the demo (all 9 rows are the 08:18 seed sweep). Fix chip: URL-driven mode |
| "Want to do online" | feature | Stub: 2026-07-22-online-sales-channel-stub.md + brainstorm chip |
| "Chinese/Japanese/Korean" + games list (Lorcana, One Piece, MTG, Yu-Gi-Oh!, Topps/Panini) | feature (one cluster) | Stub: 2026-07-22-multi-game-multi-language-catalogue-stub.md + brainstorm chip (both restructure the same catalogue/pricing pipeline) |
| "Pricing feature for conditions" | feature | Stub: 2026-07-22-condition-pricing-stub.md + brainstorm chip. Verified: condition exists on items but calculateSellPrice/calculateBuyPrice are condition-blind |
| "Live auctions: QR codes… figure how scanner would actually work" | feature + question | Scanner question answered in triage (per-item UUID QR labels + keyboard-wedge scanners + POS SearchBar UUID detection already work). Auction pull-mode stub: 2026-07-22-live-auction-qr-pull-stub.md + brainstorm chip |
| "Connect it to their card payment system" | feature | Stub: 2026-07-22-card-terminal-integration-stub.md + brainstorm chip. First question for the shop: which terminal do they own |
| "IDEA: display tablet for customer use" | feature | Stub: 2026-07-22-customer-kiosk-stub.md + brainstorm chip. Read as buylist intake (ambiguity flagged in stub — confirm with the shop) |

Repro environment note: verified against a throwaway NODE_ENV=test DB (tests/e2e seed) on
:3100; the staged "Cardtill Demo" DB was only read, never written. All temporary config edits
(next.config.ts distDir override, launch.json e2e entry, tsconfig auto-include) were reverted.
