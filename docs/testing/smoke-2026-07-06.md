# Smoke testing — 2026-07-06

Raw findings log (verbatim from owner usage testing):

- On any of the search functions - what if you don't know the full spelling of the Pokemon?
- Is a catalogue of all the cards needed, EG as an easy way to see all of the cards on the market - when buying, if you search a card it takes a
- So how does the QR code scanner work, once scanned how does the card come up - does come up on the POS tab
- What's the use of the print QR label in the buy function, w
- We need to change the names of the tabs to - Sales, Purchasing,
- On the buy page after searching the incorrect spelling the search function froze and didn't say no card found or let you change the spelling / search another function
- Ive noticed that the prices search returns more versions of snorlax than the buy function does - is this the same across all cards
- Same card showing for - Snorlax cards:
  - Flashfire · #80
  - Generations · #58

## Triage

| # | Item | Kind | Outcome |
|---|------|------|---------|
| 1 | Search without knowing full spelling | Feature | Part of **search overhaul** work item (chip) |
| 2 | Catalogue of all cards to browse | Feature | Stub spec in `docs/superpowers/catalogue-browser.md` + brainstorm chip. Entry truncated ("…it takes a") — needs completion |
| 3 | How does QR scanning work? | Question | Answered: labels carry a UUID; scanning into the POS search box looks up the inventory item and shows the card with in-stock options on the POS tab |
| 4 | Use of print QR label in buy? | Question | Answered: prints one label per card bought so new stock can be scanned at POS later. Entry truncated ("…w") — needs completion |
| 5 | Rename tabs (Sales, Purchasing, …) | Tweak | Blocked — list incomplete, need full old→new mapping. Current tabs: POS, Buy, Customers, Wants, Inventory, Prices, Reports, Settings (`components/layout/Nav.tsx`) |
| 6 | Buy search froze on misspelling | Bug | Confirmed. Zero local hits → live Pokémon TCG API fallback with **no timeout** (`lib/apis/pokemon-tcg.ts`), input disabled while loading. Part of **search overhaul** chip |
| 7 | Prices shows more Snorlax versions than buy | Bug | Confirmed. Buy page caps displayed results at 10 (`app/(app)/buylist/page.tsx` `cards.slice(0, 10)`); prices page returns up to 30 from the live API. Yes — same for every card with >10 printings. Part of **search overhaul** chip |
| 8 | Same Snorlax card twice (Flashfire #80 / Generations #58) | Not a bug | Verified in DB (57 distinct Snorlax rows, zero duplicate external IDs or shared images) and visually: Generations #58 is a legitimate reprint of Flashfire #80 with identical artwork. Distinct cards, potentially different market prices |

### Work items spun off

1. **Search overhaul** (items 1, 6, 7): fix freeze via API timeout + abort, add proper no-results/error states, fuzzy matching, raise/paginate the 10-result cap, and decide whether buy + prices should share one search backend.
2. **Catalogue browser** (item 2): brainstorm session from stub spec.

### Awaiting user input

- Full tab rename mapping (item 5) — then a quick batch tweak.
- Completion of truncated entries in items 2 and 4.
